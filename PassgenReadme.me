# CLI Password Generator

This is a command-line interface (CLI) tool that generates passwords for different SSID formats. It provides a user-friendly way to generate passwords based on the specific SSID (Wi-Fi network name) provided.

## Requirements

- Python 3.x

## Usage

1. Clone the repository or download the `password_generator.py` file to your local machine.

2. Open a terminal or command prompt and navigate to the directory where `password_generator.py` is located.

3. Run the following command:
